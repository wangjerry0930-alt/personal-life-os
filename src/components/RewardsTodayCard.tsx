import Icon from './Icon';
import {getGamingAllowance,getRewardState} from '../services/rewardService';
export default function RewardsTodayCard({onOpen}:{onOpen:()=>void}){const allowance=getGamingAllowance();const rewards=getRewardState();const remaining=Math.max(0,allowance.baseMinutes+allowance.activatedVoucherMinutes+allowance.bonusMinutes-allowance.usedMinutes);return <section className="rewards-today-card"><div><span className="pill teal">TODAY’S GAMING</span><div className="rewards-today-stats"><span>Free <b>{allowance.baseMinutes}m</b></span><span>Activated <b>{allowance.activatedVoucherMinutes}m</b></span><span>Used <b>{allowance.usedMinutes}m</b></span><span>Remaining <b>{remaining}m</b></span></div></div><div className="rewards-today-side"><b>{rewards.credits}</b><small>Credits</small><button className="secondary" onClick={onOpen}>Open Rewards <Icon name="ArrowRight" size={13}/></button></div></section>}
