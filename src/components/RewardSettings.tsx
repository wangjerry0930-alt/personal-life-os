import {useState} from 'react';
import {getRewardState,updateRewardConfig} from '../services/rewardService';
export default function RewardSettings(){const config=getRewardState().config;const[weekday,setWeekday]=useState(config.weekdayActivationCap);const[weekend,setWeekend]=useState(config.weekendActivationCap);const[message,setMessage]=useState('');const save=()=>{updateRewardConfig({weekdayActivationCap:Math.max(0,weekday),weekendActivationCap:Math.max(0,weekend)});setMessage('Reward settings saved.');setTimeout(()=>setMessage(''),1500)};return <section className="settings-card reward-settings"><div className="settings-card-head"><div><h3>Rewards & Gaming</h3><p>Configure how much purchased game time can be activated per day.</p></div></div><div className="reward-settings-grid"><label>Weekday cap (minutes)<input type="number" min="0" value={weekday} onChange={e=>setWeekday(Number(e.target.value))}/></label><label>Weekend cap (minutes)<input type="number" min="0" value={weekend} onChange={e=>setWeekend(Number(e.target.value))}/></label></div><button className="primary" onClick={save}>Save reward settings</button>{message&&<span className="muted">{message}</span>}</section>}
