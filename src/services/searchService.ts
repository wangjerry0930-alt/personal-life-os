import type { AppData } from '../domain/types';
import { captureRepository, knowledgeRepository, peopleRepository, personFactRepository, resourceRepository } from '../repositories/legacyRepositories';
import { loadBooks } from '../repositories/bookRepository';

export type SearchResult={id:string;name:string;type:string;meta?:string};
export function searchAcrossLifeOS(data:AppData,query:string):SearchResult[]{const q=query.toLowerCase().trim();if(!q)return[];const captures=captureRepository.load().map(x=>({id:x.id,name:x.text,type:'Quick capture',meta:x.topics?.join(' · ')}));const people=peopleRepository.load().map(x=>({id:x.id,name:x.name,type:'Person',meta:[x.relationship,...(x.interests||[])].filter(Boolean).join(' · ')}));const facts=personFactRepository.load().map(x=>({id:x.id,name:x.fact,type:'Person fact'}));const knowledge=knowledgeRepository.load().map(x=>({id:x.id,name:x.title,type:'Knowledge',meta:x.field}));const books=loadBooks().map(x=>({id:x.id,name:x.title,type:'Book'}));const resources=resourceRepository.load().map(x=>({id:x.id,name:x.title,type:'Resource',meta:x.type}));const all:SearchResult[]=[...data.areas.map(x=>({id:x.id,name:x.name,type:'Learning area',meta:x.focus})),...data.skills.map(x=>({id:x.id,name:x.name,type:'Skill',meta:x.focus})),...data.tasks.map(x=>({id:x.id,name:x.title,type:'Task',meta:x.category})),...(data.projects||[]).map(x=>({id:x.id,name:x.name,type:'Project'})),...(data.goals||[]).map(x=>({id:x.id,name:x.title,type:'Goal'})),...people,...facts,...knowledge,...books,...resources,...captures];return all.filter(item=>(item.name+' '+(item.meta||'')).toLowerCase().includes(q)).slice(0,80);}
