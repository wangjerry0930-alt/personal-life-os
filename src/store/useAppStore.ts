import{useEffect,useState}from'react';import type{AppData,Goal,LearningArea,Project,Skill}from'../domain/types';import{seed}from'../domain/seed';const KEY='personal-life-os-data';const STREAK_RESET='personal-life-os-streak-reset-v1';
export function useAppStore(){const[data,setData]=useState<AppData>(()=>{try{const saved=JSON.parse(localStorage.getItem(KEY)||'null') as AppData|null;if(!saved)return seed;if(!localStorage.getItem(STREAK_RESET)){localStorage.setItem(STREAK_RESET,'1');return{...saved,habits:saved.habits.map(h=>({...h,streak:0,best:0,checked:false}))}}return saved}catch{return seed}});useEffect(()=>localStorage.setItem(KEY,JSON.stringify(data)),[data]);useEffect(()=>{document.documentElement.style.setProperty('--current-streak',String(Math.max(0,...data.habits.map(h=>h.streak))));document.documentElement.style.setProperty('--best-streak',String(Math.max(0,...data.habits.map(h=>h.best))))},[data.habits]);
const toggleTask=(id:string)=>setData(d=>{const task=d.tasks.find(t=>t.id===id);if(!task)return d;const done=task.status!=='done';return{...d,tasks:d.tasks.map(t=>t.id===id?{...t,status:done?'done':'todo',completedAt:done?'Now':undefined}:t),activities:done?[{id:Date.now().toString(),label:task.title,type:'Task completed',time:'Just now',meta:'Daily progress'},...d.activities]:d.activities}});
const toggleHabit=(id:string)=>setData(d=>({...d,habits:d.habits.map(h=>h.id===id?{...h,checked:!h.checked,streak:!h.checked?h.streak+1:h.streak}:h)}));const updateLog=(field:keyof AppData['log'],value:string|number)=>setData(d=>({...d,log:{...d.log,[field]:value}}));const addArea=(name:string)=>setData(d=>{const area:LearningArea={id:`area-${Date.now()}`,name,description:'A new direction to explore and develop.',priority:'Medium',status:'Active',progress:0,focus:'Choose a current focus',topics:[],color:'#8b7cf6'};return{...d,areas:[...d.areas,area]}});const addSkill=(name:string)=>setData(d=>{const skill:Skill={id:`skill-${Date.now()}`,name,level:'Beginner',target:'Intermediate',practiceCount:0,minutes:0,lastPracticed:'Not yet',focus:'Choose what to practice next',progress:0};return{...d,skills:[...d.skills,skill]}});const deleteArea=(id:string)=>setData(d=>({...d,areas:d.areas.filter(a=>a.id!==id)}));const deleteSkill=(id:string)=>setData(d=>({...d,skills:d.skills.filter(s=>s.id!==id)}));const practiceSkill=(id:string,minutes:number)=>setData(d=>({...d,skills:d.skills.map(s=>s.id===id?{...s,practiceCount:s.practiceCount+1,minutes:s.minutes+minutes,lastPracticed:'Today',progress:Math.min(100,s.progress+2)}:s),activities:[{id:Date.now().toString(),label:'Practiced a skill',type:'Skill practice',time:'Just now',meta:d.skills.find(s=>s.id===id)?.name||'Skill'},...d.activities]}));const addProject=(name:string)=>setData(d=>({...d,projects:[...d.projects,{id:`project-${Date.now()}`,name,type:'Personal project',status:'Planning',progress:0,deadline:'No deadline',meta:'New project',next:'Define the next step',color:'#8b7cf6'}]}));const addGoal=(title:string)=>setData(d=>({...d,goals:[...d.goals,{id:`goal-${Date.now()}`,title,period:'Personal goal',progress:0,detail:'Just started'}]}));const deleteProject=(id:string)=>setData(d=>({...d,projects:d.projects.filter(p=>p.id!==id)}));const deleteGoal=(id:string)=>setData(d=>({...d,goals:d.goals.filter(g=>g.id!==id)}));const adjustTaskDifficulty=(id:string)=>setData(d=>{const levels=['Easy','Medium','Hard'] as const;return{...d,tasks:d.tasks.map(t=>{if(t.id!==id)return t;const next=levels[(levels.indexOf(t.difficulty)+1)%levels.length];return{...t,difficulty:next}})}});const autoTuneDifficulty=()=>setData(d=>{const daily=d.tasks.filter(t=>t.category==='Daily progress');const done=daily.filter(t=>t.status==='done').length;const ratio=done/Math.max(daily.length,1);const levels=['Easy','Medium','Hard'] as const;const direction=ratio>=.7?1:ratio<=.3?-1:0;if(!direction)return d;return{...d,tasks:d.tasks.map(t=>{if(t.category!=='Daily progress'||t.status==='done')return t;const index=levels.indexOf(t.difficulty);return{...t,difficulty:levels[Math.max(0,Math.min(2,index+direction))]}})}});const generateDailyTasks=(force=false)=>setData(d=>{const base=force?{...d,tasks:d.tasks.filter(t=>t.category!=='Daily progress'||t.status==='done')}:d;const existing=base.tasks.filter(t=>t.category==='Daily progress').length;if(existing>=6)return d;const area=base.areas.find(a=>a.status==='Active');const skill=base.skills.find(s=>s.progress<100);const additions=[area&&{id:`task-${Date.now()}-area`,title:`Review one ${area.name} concept`,description:`Spend 15 minutes on your current focus: ${area.focus}.`,minutes:15,difficulty:'Easy' as const,category:'Daily progress' as const,areaId:area.id,status:'todo' as const},skill&&{id:`task-${Date.now()}-skill`,title:`Practice ${skill.name} for 20 minutes`,description:`Make one small exercise around: ${skill.focus}.`,minutes:20,difficulty:'Easy' as const,category:'Daily progress' as const,skillId:skill.id,status:'todo' as const}].filter(Boolean) as any[];return{...base,tasks:[...base.tasks,...additions]}});return{data,setData,toggleTask,toggleHabit,updateLog,addArea,addSkill,deleteArea,deleteSkill,practiceSkill,addProject,addGoal,deleteProject,deleteGoal,generateDailyTasks,adjustTaskDifficulty,autoTuneDifficulty}}
